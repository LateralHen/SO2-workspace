#include <stdio.h>

int main() {
    // Scrive direttamente sul terminale usando printf
    printf("Questo va sul terminale con printf.\n");

    // Scrive direttamente sul terminale usando fprintf su stdout
    fprintf(stdout, "Questo va sul terminale con fprintf.\n");

    return 0;
}
